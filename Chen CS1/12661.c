#include <stdio.h>


int main() {

    int testcase;
    scanf("%d", &testcase);

    while (testcase--) {
        int n, m, k;
        scanf("%d %d %d", &n, &m, &k);

        int num[n];
        for (int i = 0; i < n; i++)
            scanf("%d", &num[i]);
        
        
    }
    

    return 0;
}